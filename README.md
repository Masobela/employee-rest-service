# Employee REST Service

A simple Spring Boot RESTful web service for managing employee data.  
Built as part of the Hewlett Packard Enterprise (HPE) Software Engineering Virtual Experience on Forage.

## 🚀 Features
- REST API built with Spring Boot
- Endpoints to list employees
- JSON responses
- Easily extendable to connect with a database

## 📂 Project Structure
- `Employee.java` – Employee entity
- `Employees.java` – Wrapper for employee list
- `EmployeeController.java` – REST controller exposing endpoints
- `EmployeeManager.java` – Service layer managing employee data
- `RestServiceApplication.java` – Main Spring Boot application entry point

## ▶️ Running the Application
1. Clone the repository:
   ```bash
   git clone https://github.com/Masobela/employee-rest-service.git
